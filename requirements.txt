# The LaunchDarkly server-side SDK for Python. This is the only LaunchDarkly
# dependency the demo needs: streaming updates, the targeting engine, and the
# flag change listener used in ld_client.py are all built in.
launchdarkly-server-sdk>=9.16.1,<10

# Web framework for the demo landing page. Flask's built-in server is threaded,
# which is all the Server-Sent Events stream in app.py requires.
flask>=3.0,<4

# Loads the SDK key and other settings from your local .env file.
python-dotenv>=1.0

# Used by scripts/setup_launchdarkly.py to create the flag and its targeting
# rules through the LaunchDarkly REST API. Not needed to run the app itself.
requests>=2.31
